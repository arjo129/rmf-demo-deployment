---
driver:
  name: vagrant
  #network:
  #- ["forwarded_port", {guest: 80, host: 8080}]

provisioner:
  name: chef_solo
  product_name: cinc
  product_version: 16

verifier:
  name: inspec

platforms:
  - name: ubuntu-20.04

suites:
  - name: server
    attributes:
    data_bags_path: "test/integration/data_bags"
    verifier:
      inspec_tests:
        - test/integration/server
